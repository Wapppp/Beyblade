import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Colors.orange;
  static const Color appBarColor = Colors.black;
  static const Color scaffoldBackgroundColor = Colors.grey;
  static const Color cardColor = Color.fromARGB(255, 56, 56,
      56); // Adjusted to match the darker shade of grey used in the RegisterPage
  static const Color googleButtonColor =
      Colors.red; // Color for Google sign-in button
}
